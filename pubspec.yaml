name: youapp_clean_architecture
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

scripts:
  convert: dart run bin/convert_artools.dart

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  get: ^4.6.6
  image_picker: ^1.1.2
  intl: ^0.20.2
  artools: ^0.0.3+5
  dio_cache_interceptor: ^3.5.1
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.2
  flutter_svg: ^2.0.17
  animated_text_kit: ^4.2.3
  shared_preferences: ^2.5.1
  equatable: ^2.0.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mocktail: ^1.0.3
  change_app_package_name: ^1.5.0
  flutter_lints: ^4.0.0


flutter:

  uses-material-design: true
